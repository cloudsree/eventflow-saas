import { db } from "@/lib/db"; import { money } from "@/lib/utils"; import { addBudgetItem } from "@/features/budget/actions";
export default async function BudgetPage({params}:{params:{eventId:string}}){const items=await db.budgetItem.findMany({where:{eventId:params.eventId}});const est=items.reduce((s,i)=>s+Number(i.estimatedAmount),0);const act=items.reduce((s,i)=>s+Number(i.actualAmount||0),0);return <div><h1 className="text-3xl font-bold">Budget</h1><div className="mt-4 grid gap-4 md:grid-cols-2"><div className="rounded-2xl border bg-white p-5"><p className="text-slate-500">Estimated</p><p className="text-3xl font-bold">{money(est)}</p></div><div className="rounded-2xl border bg-white p-5"><p className="text-slate-500">Actual</p><p className="text-3xl font-bold">{money(act)}</p></div></div><form action={addBudgetItem.bind(null,params.eventId)} className="mt-6 grid gap-3 rounded-2xl border bg-white p-4 md:grid-cols-5"><input name="category" placeholder="Category" className="rounded-lg border p-2"/><input name="name" placeholder="Item" className="rounded-lg border p-2" required/><input name="estimatedAmount" placeholder="Estimated" className="rounded-lg border p-2"/><input name="actualAmount" placeholder="Actual" className="rounded-lg border p-2"/><button className="rounded-lg bg-slate-950 px-4 py-2 text-white">Add</button></form><div className="mt-6 overflow-hidden rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-100"><tr><th className="p-3">Category</th><th>Name</th><th>Estimated</th><th>Actual</th></tr></thead><tbody>{items.map(i=><tr className="border-t" key={i.id}><td className="p-3">{i.category}</td><td>{i.name}</td><td>{money(i.estimatedAmount)}</td><td>{money(i.actualAmount)}</td></tr>)}</tbody></table></div></div>}
